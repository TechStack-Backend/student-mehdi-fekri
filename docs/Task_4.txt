1. What are the main advantages of using CBVs?
امکان اسفتاده مجدد از کد را فراهم میسازد.
باعث سازماندهی بهتر کد و ماژولار شدن آن میشود.
با استفاده از CBVمیتوانیم کدخود را اکستند کنیم برای نیاز های خودمون.
خود CBV جنگو فانکشن ها و امکانات از پیش ساخته ای داره که میتونه خیلی کمک کندده باشه.
2. What are the roles of the generic views: ListView, DetailView, CreateView,
UpdateView, and DeleteView?
خب اینا همون Built-in Functionality های جنگو هستن که در قسمت قبلی بهشون اشاره کردم.خب این ویو ها میتونن بسیاری از کارهارو برام ما راحت تر کنن.
ListView:این آبجکت هارو برامون لیست میکنه
DetailView:خب این طلاعات یک آبجکت خاص رو برامون نمایش میده
CreateView:خب از این برای وارد کردن اطلاعات جدید استفاده میکنیم.
به طوری که اول فرم رو نشون میده و اطلاعات رو میگیره و اعتبارسنجی میکنه و در دیتابیس ذخیره میکنه.
UpdateView:همونطور که از اسمش پیداست برای ادیت کردن آبجکت ها میتونیم ازش استفاده کنیم که همه آبجکت هارو برامون نمایش میده و میتونیم آپدیت کنیم و سپس ذخیره کنیم
DeleteView:اینم یک ویو هستش که برای دیلیت کردن آبجکت ها استفاده میشود

3. What is the purpose of get_queryset() and form_valid() methods, and how can you
override them?
با استفاده از get_queryset میگوییم که چه دیتا هایی باید از دیتابیس بازیابی بشن .حالا اگه بخواهیم مثلا دیتاهارو فیلتر کنیم یا ترتیب نشان دادن اون هارو عوض کنیم و همچین کارهایی باید اون رو override کنیم.
یکی دیگه از متد های مهم تو جنریک ویو هاform_valid()است. وقتی که فرمی validتشخصی داده بشه این متد فرخوانی میشه.درحالت عادی این متد میاد فرم رو ذخیره میکنه و کاربر رو به جای درست ریدایرکت میکنه.حالا کارهایی که با این متد میتونیم انجام بدیم اینه که مثلا قبل از ذخیره فرم خودمون اگه خواستیم اطلاهات دیگه اضافه کنیم یا از عملبان ها لاگ بگیریم و این جور کارهارو میتونیم به خوبی انجام بدیم
4.How does form.is_valid() work, and when is form_valid() triggered?
خب ما وقتی میخواییم یک فرم رو اعتبار سنجی کنیم از is_valid استفاده میکنیم و وقتی که همه چیز به درستی وارد شده بود و این متد True  رو برگردوند متد form_validصدا زده میشه.
5. How do you connect CBVs to URLs using the as_view() method?
خب قبلا ما یک ویو داشتیم که تابع بود و به راحتی میومدیم تو urlpatterns اون ویو رو با تابع path اضافه میکردیم،
حالا چون کلاس داریم این کارو رو نمیتونیم انجام بدیم.
برای هندل کردن این مشکل متد as_view() به کمک ما میاد.مثلا اگر فرض کنیم که یک متد get در داخل کلاس داریم و میخواییم مسیر این CBV رو توی urls وارد کنیم، از متد as_view کلاس ویو استفاده میکنیم و این خودش مشکل مارو هندل میکنه
6. How do you connect a form to a CBV (e.g., CreateView or UpdateView)?
خب اول کار که یک فرمی رو تعریف میکنیم و حالا این فرم رو میتونیم هر جور که خودمون میتونمی تعریف کنیم.
بعد از تعریف کردن فرم حالا کلاس ویو رو تعریف میکنیم.
تو این کلاس ما میتونیم بگیم که این ویو مربوط به کدوم مدل و کدوم فرم هست و بعدش بگیم که اگر فرم رو درست پر کرد به چه صفحه ای بره .
بعد از این کار هم مثل سوال قبلی می آییم ویو رو اد میکنیم تو مسیر ها .به همین راحتی.
7. How can you perform field-level validation using the clean_<fieldname>() method?
اول درمورد خود field-level validation توضیح بدم
درواقع به همین اعتبارسنجی هر فیلد از فرم رو field-level validation میگن و اگه این اعتبارسنجی با شکست مواجه بشه تابع is_valid ،خروجیش False خواهد بود .حالا موقع ساختن فرم ما وقتی محتوای هم فیلد رو مشخص میکنیم که مثلا ایمیل باشه یا ورودی فقط عدد باشه، به همین ربط داره و اون توابع این کار اعتبارسنجی رو انجام میدن.
حالا ما میتونیم این توابعی که کار اعتبارسنجی رو انجام میدن رو override کنیم و چند تا شرط دیگه هم مثلا بهش اضافه کنیم که اونارو هم اعمال کنه 
برای این کار تو کلاسمون متودی با این فرمتی که گفته شد تعریف میکنیم ، مثلا clean_username  و بعدش با تابع cleaned_data هم میتونیم مقدار اون فیلد رو بدست بیاریم و بعدش کاری که میخواییم بکنیم رو انجام بدیم.
What is the difference between clean() (form-level) and clean_<fieldname>()
(field-level) validation?
خپ
همانطور که در سوال قبلی توضیح دادم clean_<fieldname>() برای اعتبارسنجی یک فیلد خاص از فرم به کار میره  و وقتی از is_valid استفاده میکنیم به صورت اوتوماتیک صدا زده میشه ،درحالی که تابع clean برای اعتبارسنجی کل فرم استفاده میشه و برای اعتبار سنجی یچند فیلد هم میتونه استفاده یشه و هدف از آن چک کردن رابطه بین فیلد ها مثلا اینکه دوتا فیلد داریم که اولی پسوورد رو میگیره و فیلد بعدیش هم میگه دوباره پسوورد رو وارد کن،‌برای هندل کردن این که پسوورد های ورودی با هم یکی باشن از clean استفاده میکنیم.	
9. How can you handle validation errors and display them in templates?
خب تو جنگو دستور هایی داریم که خطاهارو برای هر فیلد نشون میده و میتونیم از یک دستور استفاده کنیم که خطا های کلی رو نشون بده .
اگر رهم فرم نامعتبر باشه همان فرم با همان خطا های خودش به تمپلیت فرستاده می شود 
خطا های هر فیلد در form.field.errors قرار میگیرن و خطا های کلی در form.non_field_errors قرار میگیرن.
10. How can you use Django’s messages framework to give users feedback after create,
update, or delete actions?
معمولا برای نشون دادن پیام های موقت به کاربر استفاده میشه .
تابع های آماده داره و میتونیم ازش استفاده کنیم.
تو ویو های خودمون هم میتونیم ازش استفاده کنیم .
مثلا وقتی فرم رو با موفقیت پر کرد و یه چیزی رو اضافه کرد میتونیم از این برای نشون دادن یه پیام موفق یا ناموفق بودن کارش رو براش نشون بدیم .
11. How can organizing your code make it easier to maintain and extend in larger projects?
حالا هر چقدر کد منظم و با سازمان تر باشه دیباگ و فهمیدن اون کد راحت تره .
میشه به خوبی تقسیم وظایف کرد .
قابلیت استفاده مجدد داره.گسترش راحت تر کد
حالا چجوری این کار رو انجام بدیم؟
به جای یک ویو بزرگ چند تا ویو کوچک داشته باشیم
کلا دسته بندی فایل هارو بهتر انجام بدیم
هر اپ کار مخصوص خودش رو انجام بده 
اینطوری تغییرش هم راحت تره 
12. Search about CSRF tokens. Why they are important?

خب تو این بخش قراره وارد امنیت بشیم که من خیلی دوسش دارم 
منظور از CSRF Cross-Site Request Forgery اینه که ما وقتی که توی یه سایتی هستیم و هنوز لاگ اوت نکردیم و یک سایت دیگه همین لحظه تلاش میکنه که خودش رو جای ما بزاره و اطلاعات مارو دزدی کنه 
حالا جنگو برای حل کردن این از یک توکن استفاده میکنه که برای هر درخواست یک توکن تولید میکنه و این توکن تو تمپلیت هم اضافه میشه و وقتی درخواست ارسال میشه ،‌سرور چک میکنه که درخواست معتبر هست یا نه 
