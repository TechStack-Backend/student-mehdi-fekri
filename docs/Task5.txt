1. What is Django’s built-in authentication system, and what problems does it solve?
سیستم احراز هویت جنگو دو کار مهم را هندل میکنه که یکی احراز هویت و یکی هم تعیین سطح دسترسی هستش و خود جنگو تمامی امکانات برای هندل کردن اکانت ها و گروه ها و مجوز هاو session هارو به صورت پیش فرض داره. و با استفاده از سیستم احراز هویت جنگو میتونیم یک سیستم احراز هویتی رو از صفر تا صد پیاده سازی کنیم.
خب از مزایای این سیستم جنگو میتوان به این ها اشاره کرد که رمز عبور هارو به صورت هش شده و با استفاده از salt ذخیره میکنه و در صورت هک شدن هم رمز ها فاش نمیشوند و session هارو به خوبی هندل میکنه و کاربر رو پس از لاگین در پیج های مختلف نگه میداره و همچنین میتونیم مدیریت دسترسی هم داشته باشیم و همچنین جنگو فرم ها و ویو های آماده ای برای ورود و خروج و تغییر رمز عبور و بازیابی رمز عبور و این جور کارهایی که تو هر سیستمی انجام مییشه .

2. How web applications save the password and why?

خب منطقیه که وب اپلیکشین نیاد که پسوورد ها و یوسر نیم هارو به صورت متن مستقیم ذخیره کنه و میاد از هش استفاده میکنه ولی خب بازم ممکنه که از هش ها هم چیزی درآورد از salting استفاده میکنن و موقع هش کردن بهش یک استرینگی اضافه میشه و این باعث هم میشه که اگر دو رشته هش یکسان داشته باشند با استفاده از نمکی که به اصطلاح اضافه میشه هش نهایی آنها متفاوت باشه.
3. What’s the difference between hashing, encoding, and encryption?
خب از هش شروع کنیم که هشینگ یک فرآیند یک طرفه هستش و امکان بازگشت از هش به متن اصلی رو نداریم و برای مقایسه هم می آییم هش های اونارو مقایسه میکنیم و   یک طول ثابت داره و رمزگشایی یعنی نداره
حالا encoding که کدگزاری میشه یک فرآیند دو طرفه هستش یعنی decoding  هم داریم و هذف آن مخفی سازی نیست و یک نوع تغییر شکل هست 
ولی درآخر encryption یا همان رمزنگاری برای محرمانگی استفاده میشه و با استفاده از کلید هایی این رمز نگاری انجام میشه و این کلید هارو طرف های مبدا و مقصد دارند و بعضی موقع ها کلید های عمومی داریم که اینو همه میتونن داشته باشن اگر اتکر هم بهش دسترسی پیدا کنه اتفاق خاصی نمیتونه بیوفته و چون ای برای رمزنگاری استفاده میشه و برای رمزگشایی یک رمز خصوصی داریم که اینو فق مقصد داره و با استفاده از این متن فرستاده شده رو رمز گشایی میکنه.
4. What are sessions and cookies, and how do they help keep users logged in?


خب HTTP در واقع stateless هست .یعنی چیزی رو ذخیره نمیکنه و بعد از تموم شدن یک درخواست تمامی اطلاعات رو از بین میبره  و اونارو به یاد نمیسپره.حالا اگر درباره sessionبخوام بگم اینا یک رکوردی هست که سمت سرور ذخیره میشه و وضعیت اطلاعات یک کاربر رو نگه میداره و برای هر فرد یک session ID متفاوت داریم .حالا Cookies یک فایل متنی که سرور اونو ایجاد میکنه و به مرورگر کلاینت میفرسته و تا درسمت کلاینت ذخیره بشه و سرور اینو به هر درخواست کلاینت میفرستد و مهم ترین کوکی هم کوکی هست که session ID هم با آن فرستاده میشود و حالا در فرآیند لاگین بخوام توضیح بدم اینطوری هستش که وقتی کاربری لاگین میکنه یک session براش ساخته میشه و کوکی به مروگر ارسال میشه و در درخواست های بعدی وقتی سرور میبینه درخواستی از طریق کلاینتی با این session ID اومده وضعیت لاگین و داده های مخصوص اونو بارگذاری میکنه .
5. Where do sessions save?
داده های session در سمت سرور ذخیره میشوند و درحالی که کوکی ها در مرورگر کلاینت ذخیره میشن و دلیل اونم اینه که به دلیل امنیتش که اتکر میتونه اونو دستکاری کنه و به اطلاعات یکی دیگه دسترسی پیدا کنه.
6. What are the main authentication views Django provides (e.g., LoginView, LogoutView,
PasswordChangeView)?


اول کار هم یه توضیح کوچیک دادم که جنگو فرم ها و ویوهایی برای کارهای روتینی که در هنگام لاگین انجام میدیم رو داره و تمامی منطق ها راجب به احراز هویت رو داره.
یکی از اینا Login view هست که برای ورود و اعتبارسنجی و ایجاد نشست هست
Logout view :
برای خروجی کاربر و پاک کردن اطلاعات نشست کاربر و هدایت او به صفحه مورد نظر 
PasswordChangeView:
برای تغییر رمز عبور و باید کاربر در حالت لاگ این باشه 
و چند تا ویوهای کاربری دیگه 
 
7. How can we restrict access to certain pages (for example, using @login_required or
LoginRequiredMixin)?
خب ما محدودیت هایی داریم که وقتی تا کاربر لاگین نکرده نتونه بعضی از صفحات رو ببینه و یه چیز منطقی هستش که این محدودیت هارو داشته باشیم و میتونیم برای این از دو روش استفاده FBV ها و CBV ها استفاده کنیم و برای FBV میتونیم روش رایجتری که استفاده میشه به کار ببریم و اونم استفاده از دیکریتور ها هستش که از @login_required استفاده کنیم و کافیه که فقط اون دیکریتور رو به بالای تابعمون اضافه کنیم و اگر کاربری بخواد به این ویو دسترسی پیدا کنه به طور خودکار اونو به صفحه لاگین دایرکت میکنه و پس از لاگین دوباره به صفحه ای که میخواسته بره ری دایرکت میکنه 
روش بعدی هم موقعی هستش که از CBV ها استفاده میکنیم و اینجا باید LoginRequiredMixin استفاده کنیم.خود میکسین ها برای تزریق قابلیت هایی به کلاس ها استفاده میشوند و کلاس ما باید از LoginRequiredMixin ارث بری کند 

8. How can we create a custom user registration form?
خب میتونیم از فرم های خود جنگو استفاده کنیم و باید از UserCreationForm ارث ببره و بازم هم خودمون میتونمی فیلد های اضافه ای رو هم اضافه کنیم و قابلیت کاستومایز کردن رو داره یعنی و حالا باید یک ویو بسازیم و از اون فرم استفاده کنیم 
9. What is Django default user model?
مدل پیشفرض یک کلاسی هست به اسم User که برای مدیریت کاربر ها و هویت آنها استفاده میشود و فیلد های کاربردی مثل username, password,email,last_name,first_name,is_active,is_staff,is_superuser,data_joined رو داره.

10. How we can extend Django default user model? 
خب حالا مثل بیشتر متود های آماده جنگو ما میتونیم این رو هم اکستندش بدیم و فیلدهای جدیدی بهش اضافه کنیم .اینجا دو روش داریم .
روش رایج تر و بهتر اینکه که بیاییم یک مدل کاربر بسازیم و از یکی از دو کلاس اصلی جنگو استفاده کنیم .
اگر بخواهین فیلد های پیش فرض جنگو مثل نام کاربری یا ایمیل و نام و نام خانوادگی رو حفظ کنیم و فقط فیلد های جدید اضافه کنیم از کلاس AbstractUser استفاده میکنیم و از اون ارث میبریم و برای این کار هم باید در settings.py هم تغییراتی رو اضافه کنیم و AUTH_USER_MODEL رو برابر با users.CustomUser قرار بدهیم و روش دوم هم به این صورت است که برای موقعی استفاده میشود که مدل ما از قبل تعریف شد و پروژه در حال اجرا هست و میخواهیم فیلد هایی رو اضافه کنیم و برای این کار یک مدل جدید میسازیم و با استفاده از one to one filed به مدل مورد نظر متصل کنیم.
یک روش دیگری هم داریم به اسم  proxy که برای تغییر رفتار مناسب است نه تغییر داده. در کل روش بهتر همان روش اول و استفاده از abstractuser هست.
11. How can we connect the User model to other models (like linking a Developer to a
User)?

خب این درباره این مورد یکمی در سوال قبل توضیح دادم .گفتیم که میتونیم از OneToOneField استفاده کنیم .این روش موقعی استفاده میشه که بخوایم جزئیات اضافه ای به کاربر اضافه کنیم و باید هر ردیف از مدل سفارشی دقیقا به یک کاربر مرتبط باشه و هر کاربر نیز یک ردیف از ان مدل رو داشته باشه . میتونیم از one to many  هم استفاده کنیم و یک کاربر میتونه مالک چندین نمونه از یک مدل باشه و ولی اون مدل به یک کاربر مرتبط باشه از Foreign Key اسفاده میکنیم .رابطهmany to many هم داریم.
12. Should we extend user model or use one to one relationship?
توصیه کلی و استاندارد جنگو اینو که بیاییم کاربر رو اکستند بدیم و مگر اینکه مجبور باشیم از مدل پروفایل استفاده کنیم.
در روش اکستند انعطاف پذیری بالایی داریم و عملکرد بهتری هم داره و با سیستم احراز هویت جنگو یکپارچه است در حالی که رابطه یک به یک مدل کاربر اصلی بدوت تغییر میماند ما یک مدل جدید ایجاد میکنیم و با فیلد وان تو وان به مدل خودمون متصل میکنیم و همان طور که گفتم از این روش در وسط پروژه میتونیم استفاده کنیم ولی باعث کندی عملکرد میشه و بیشتر برای داده های اختیاری مناسب هستند و نمیتوانیم فیلد های اصلی رو تغییر بدیم و خب اگه بخوام جمع بندی کنم از این دو روش وقتی میخواهیم فیلد های اختیاری مثل بیو یا اواتاری چیزی داشته باشیم یا در وسط پروژه نیاز به تغییر فیلد ها داشته باشیم از روش یک به یک استفاده میکنیم و درغیر این صورت بهتر است که از روش اکستند کردن استفاده کنیم .
13. Should we use Django default model without extending?
بله میتوانیم .ولی بهتر است که اکستندش کنیم.چون دیفالت از یوسر نیم برای ورود استفاده میکند درحالی که امروزه به دلیل امنیت از ایمیل استفاده میکنند.بیشتر برای پروژه های تمرینی و بسیار ساده از مدل دیفالت استفاده میکنیم .
14. What is different method of authentication (look for differences)? (jwt, session)
خب دو روش برای احراز هویت داریم که یکی session ها هستند که دربارش یکمی توضیح داریم و دیگری استفاده از توکن ها jwtهستند.
اول درباره session-based توضیح بدم که این روش اطلاعات کاربر رو به خاطر میسپره و در سمت سرور ذخیر میشه و کوکی حاوی این id به سرور فرستاده میشه و سرور اطلاعات مربوط به اون id رو برمیگردونه 
حالا روش دوم که استفاد از jwt هست سرور نیازی به ذخیره سازی نداره و یک توکن jwt داریم که پس از ورود کاربر سرور این توکن رو ایجاد میکنه و اون رو به کلاینت میفرسته و کاربر اون رو در کش یا یه جای دیگه ای ذخیره میکنه و در هر درخواست هم توکن به سرور ارسال میشه و نیازی به جست و جوی اون تو دیتا بیس نیست و سرور فقط امضای اونو چک میکنه.
Static & Media Files

7. What are static files and media files in Django?

خب فایل های استاتیک همان طور که از اسمش پیداست ثابت هستند و جزئی از پروژه هتسند و برای دیزاین کردن و ظاهر و عملکرد سمت کلاینت استفاده میشود و میتونه شامل CSS,JS یا عکس ها و ایکون ها و... بشه
حالا فایل های مدیا فایل هایی هستند که توسط کاربر نهایی اپلود مبشوند و از طریق فرم ها یا api ها ... تولید میشوند مثل عکس پروفایل ها داکیومنت ها و ... 
مدیریت اولی از طریق دستور collectstatic انجام میشود ولی دومی توسط وب سرور یا محیط های ابری.

8. How do we configure STATIC_URL, STATICFILES_DIRS, MEDIA_URL, and MEDIA_ROOT in
settings.py?

خب پیکربندی فایل های استاتیک اینو مشخص میکنه که جنگو فایل های تولید شده رو از کجا بخونه و چگونه تو صفحه وب نشونش بده 

STATIC_URL = 'static/'

هدف هم تعریف یک آدرس عمومی برای ارجاع به فایل های استاتیک در قالب تمپلیت
STATICFILES_DIRS
STATICFILES_DIRS = [
    BASE_DIR / 'static',  # اشاره به پوشه 'static' در ریشه پروژه
]

یک لیست از مسیر سیستمی است که جنگو باید در آن به دنبال فایل های استاتیک بگردد و این لیست برای فایل های استاتیک در سطح پروژه استفاده میشود نه داخل پوشه های استاتیک هر اپلیشکن

MEDIA_URL
هدف تعریف آدرس عمومی برای دسترسی به فایل های اپلود شده توسط کاربر
MEDIA_ROOT
هدف مسیر مطلق سیستمی که جنگو فایل های آپلود شده توسط کاربر را در آنجا ذخیره میکنه یا همان محل فیزیکی فایل ها 

9. How can we use the {% static %} tag to display CSS or images in templates?

خب ما با استفاده از این تگ برای تولید آدرس عمومی و مطلق براس دسترسی به فایل های استاتیک استفاده میکنیم و مسییر دریافتی رو کنار STATIC_URL میذاره و به فایل های استاتیک دسترسی پیدا میکنه 
خب اول کار باید کتابخونه استاتیک رو بارگذاری کنیم.
{% load static %}


<body>
    <h1>Welcome!</h1>
    
    <img src="{% static 'images/logo.png' %}" alt=" LOGO" width="150">
</body>


10. How do we allow users to upload and display profile pictures or files?
خب بعد از اینکه پیکربندی هایی رو تو فایل settings.py انجام دادیم
باید یک مدل پروفایل تعریف کنیم یک فیلد برای عکس هم داشته باشه 
avatar = models.ImageField(upload_to='profile_pics/', blank=True)

document = models.FileField(upload_to='documents/', blank=True)

خب بعدش یک فرم ایجاد میکنیم و بعدش نیاز به ویو داریم یک ویو ساده میتونیم براش بنویسیم
بعدش تو template از یک نوع رمزنگاری باید استفاده کنیم و برای نمایشش هم به این صورت نمایش میدیم 
{% if user.profile.avatar %}
    <img src="{{ user.profile.avatar.url }}" alt="Profile Picture">
{% endif %}

{% if user.profile.document %}
    <a href="{{ user.profile.document.url }}">دانلود فایل ضمیمه</a>
{% endif %}


11. What’s the difference between serving static and media files during development?

در سرویس دهی به فایل های استاتیک خود جنگو به صورت خودکار اونارو تو حالت DEBUG=True هندل میکنه و نیازی به پیکربندی urls.py نیست ولی در مورد مدیا ها باید پیکربندی کنیم 
if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
